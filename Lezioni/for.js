function main() {
    const n = 10 // impostare il valore che più si preferisce

    // conta pari
    for (let contatore = 1; contatore <= n; contatore++) { //valore iniziale, condizione di fine, incremento
        console.log(2*contatore)
    }

    //conta rovescio
    for (let contatore = 10; contatore >= 1; contatore--) {
        console.log(contatore)
    }
}
// prova
main()